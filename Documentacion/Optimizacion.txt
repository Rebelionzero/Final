Desafio:
--------
Optimizar el metodo de carga de obras, de creacion del formulario de carga, edición y de borrado.

El objetivo es lograr utilizar una sola clase, o la menor cantidad de clases posibles para manejar las obras, categorias, etc.

Ahora se utilizan muchos archivos solo para crear el formulario, la obra, editarla, etc.

Realizar un diagrama de flujo para el sistema de carga, modificacion y borrado de obras.



Desarrollo:
-----------
- Entra en obras
- verificar que existan autores, museos, categorias. Todo lo necesario para que sea posible cargar una obra.
- Si existe llamar a una clase que crea el formulario, cuando lo crea se debe verificar qeu el formulario se pida por primera vez y no como devolucion de errores.
- el mismo formulario se debe crear con las variables necesarias que alteren los valores de los inputs para cuando se de el caso de edicion.
- en right-obras cambiar el include del formulario de obras por un echo que traiga el formulario completo
- agregar un metodo mas a la clase CrearFormulario para que maneje las variables de los campos que deben estar llenos

WorkFlow:
----------
Al entrar en la vista obras.php, se verifica al igual que en todo el site que el usuario este logueado.

if( !isset( $_SESSION['Login']['autenticacion'] ) || $_SESSION['Login']['autenticacion'] === false ){
	header("Location: login.php");
}elseif( isset( $_SESSION['Login']['autenticacion'] ) && $_SESSION['Login']['autenticacion'] === true){
	include_once("../controladores/verificar_obras.php");
}

se ejecuta el || include_once("../controladores/verificar_obras.php"); || que trae los requerimientos necesarios para que se cree el formulario


Faltante:
---------
Falta explicar los pasos detras de verificar-obras.php, es necesario para poder explicar los pasos siguientes del proceso del formulario. 
Para realizar este ultimo paso es necesario analizar controlador-obras.php ay que es ese el archivo a donde se envia el formulario y verifica que los datos sean correctos, ademas es el qeu setea las variables de session que usa verficar-obras.php.

